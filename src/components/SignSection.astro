---
type Props = {
  title: string;
  content: string;
};

const { title, content } = Astro.props;
---

<section class="panel rounded-[1.75rem] px-5 py-5">
  <p class="section-kicker">{title}</p>
  <p class="mt-3 text-[0.98rem] leading-7 text-[var(--text)]/92">{content}</p>
</section>
